# Lessons

[2026-06-10] | Dispatched a subagent for plan execution; user rejected the Agent call | Execute plans inline in-session by default; don't spawn subagents unless explicitly re-confirmed at dispatch time, even if a "subagent-driven" option was picked earlier.
